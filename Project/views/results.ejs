<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Credit Score Result</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            background-color: #f4f4f4;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #333;
            color: white;
            padding: 15px 0;
        }

        header .logo h1 {
            text-align: center;
            margin: 0;
            font-size: 2em;
        }

        nav ul {
            list-style: none;
            text-align: center;
            padding: 0;
            margin: 0;
        }

        nav ul li {
            display: inline;
            margin: 0 15px;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
            font-size: 1.1em;
        }

        nav ul li a:hover {
            text-decoration: underline;
        }

        .meter-container {
            text-align: center;
            margin: 50px auto;
            padding: 30px;
            background-color: #fff;
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
            max-width: 500px;
        }

        .meter {
            width: 100%;
            height: 150px;
            background-color: #ddd;
            border-radius: 75px 75px 0 0;
            position: relative;
            margin: 0 auto;
            overflow: hidden;
            border: 2px solid #5cb85c;
        }

        .meter .fill {
            height: 100%;
            width: 100%;
            background-color: #5cb85c;
            border-radius: 75px 75px 0 0;
            position: absolute;
            top: 0;
            left: 0;
            transform-origin: left;
            transition: transform 1s ease-out;
        }

        .meter .label {
            position: absolute;
            width: 100%;
            text-align: center;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 1.5em;
            color: #333;
            font-weight: bold;
        }

        .score-display {
            margin-top: 20px;
            font-size: 2em;
            font-weight: bold;
            color: #333;
        }

        .comments {
            margin-top: 20px;
            font-size: 1.2em;
            color: #555;
        }

        .attention-button {
            background-color: #5cb85c;
            color: white;
            padding: 15px 30px;
            font-size: 1.2em;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            animation: flash 1.5s infinite;
            margin-top: 30px;
        }

        @keyframes flash {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        footer {
            text-align: center;
            padding: 15px;
            background-color: #333;
            color: white;
            position: absolute;
            bottom: 0;
            width: 100%;
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">
            <h1>Credit Score Result</h1>
        </div>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/about">About</a></li>
                <% if (user) { %>
                    <li><a href="/creditscore">Dashboard</a></li>
                    <li><a href="/logout">Logout</a></li>
                <% } else { %>
                    <li><a href="/login">Login</a></li>
                    <li><a href="/signup">Sign Up</a></li>
                <% } %>
            </ul>
        </nav>
    </header>

    <main>
        <section class="meter-container">
            <div class="meter">
                <div class="fill" style="transform: scaleX(<%= score / 1000 %>);"></div>
                <div class="label">Your Credit Score is</div>
            </div>
            <div class="score-display"><%= score %></div>
            <div class="comments">
                <% if (score < 300) { %>
                    <p>Your score is quite low. It's advisable to review your financial habits and improve your payment history.</p>
                <% } else if (score < 600) { %>
                    <p>Your score is below average. Consider improving your credit usage and making timely payments.</p>
                <% } else if (score < 800) { %>
                    <p>Your score is good. Keep up the good work with managing your credit and payments.</p>
                <% } else { %>
                    <p>Your score is excellent! You've done a great job with managing your credit and payments.</p>
                <% } %>
            </div>
            <% if (user) { %>
                <button class="attention-button" onclick="location.href='/form'">Calculate Again</button>
            <% } %>
        </section>
    </main>

    <footer>
        <p>&copy; 2024 CreditScoreTracker. All rights reserved.</p>
    </footer>
</body>
</html>
