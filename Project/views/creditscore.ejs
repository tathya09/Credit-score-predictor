<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Check Your Credit Score</title>
    <link rel="stylesheet" href="/score.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        header {
            background-color: #333;
            color: white;
            padding: 15px 0;
            position: fixed;
            width: 100%;
            top: 0;
            left: 0;
            z-index: 1000;
        }

        header .logo {
            text-align: center;
        }

        header .logo h1 {
            margin: 0;
            font-size: 2em;
        }

        nav ul {
            list-style: none;
            padding: 0;
            margin: 0;
            text-align: center;
        }

        nav ul li {
            display: inline;
            margin: 0 15px;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
            font-size: 1.1em;
        }

        main {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            width: 100%;
            background-color: #fff;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h1 {
            color: #333;
            font-size: 2.5em;
            margin-bottom: 20px;
        }

        .message {
            margin-top: 20px;
            font-size: 1.3em;
            color: #555;
        }

        .attention-button {
            background-color: #5cb85c;
            color: white;
            padding: 15px 30px;
            font-size: 1.2em;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            animation: flash 1.5s infinite;
            margin-top: 30px;
            transition: background-color 0.3s ease;
        }

        .attention-button:hover {
            background-color: #4cae4c;
        }

        .score-display {
            font-size: 2.5em;
            font-weight: bold;
            color: #333;
            margin: 20px auto;
            padding: 10px;
            background-color: #eaf4e9;
            border-radius: 10px;
            display: inline-block;
        }

        .logout-button {
            background-color: #d9534f;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 20px;
            transition: background-color 0.3s ease;
        }

        .logout-button:hover {
            background-color: #c9302c;
        }

        footer {
            text-align: center;
            padding: 15px;
            background-color: #333;
            color: white;
        }

        @keyframes flash {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.6; }
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">
            <h1>CreditScoreTracker</h1>
        </div>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/about">About</a></li>
                <% if (user) { %>
                    <li><a href="/logout" class="logout-button">Logout</a></li>
                <% } else { %>
                    <li><a href="/login">Login</a></li>
                    <li><a href="/signup">Sign Up</a></li>
                <% } %>
            </ul>
        </nav>
    </header>

    <main>
        <section class="container">
            <h1>Welcome to Your Credit Score Dashboard</h1>
            <% if (user) { %>
                <p class="message">Hi <%= user.name %>,</p>
                <% if (userInput) { %>
                    <p>Your previous credit score was:</p>
                    <div class="score-display"><%= userInput.score %></div>
                    <button class="attention-button" onclick="location.href='/form'">Calculate Again</button>
                <% } else { %>
                    <p>You haven't calculated your credit score yet.</p>
                    <button class="attention-button" onclick="location.href='/form'">Calculate Your Credit Score</button>
                <% } %>
            <% } else { %>
                <p>Please <a href="/login">log in</a> to view your credit score.</p>
            <% } %>
        </section>
    </main>

    <footer>
        <p>&copy; 2024 CreditScoreTracker. All rights reserved.</p>
    </footer>
</body>
</html>
